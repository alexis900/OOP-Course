package Java.AccountType;
import Java.Account;

public class Driver extends Account{
    public Driver(String name, String document, String email, String password, Boolean verified){
        super(name, document, email, password, verified);
    }
}
