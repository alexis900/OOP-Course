class Car {
    constructor(license, driver) { 
        this.id
        this.license = license
        this.driver = driver
        this.passagers
    }

    printDataCar = () => {
        console.log(`License: ${this.license} Driver: ${this.driver.name} Driver Document: ${this.driver.document})`);
    }
}

export default Car;